---
phase: quick-260707-g2h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/availability/route.ts
autonomous: false
requirements: [QUICK-260707-g2h]
must_haves:
  truths:
    - "On Tue Jul 7, 2026, every slot at 4 PM or later is disabled on /book"
    - "On Tue Jul 7, 2026, all slots before 4 PM stay bookable"
    - "On Tue Jul 7, 2026, a wind advisory banner appears above the slot grid"
    - "On Wed Jul 8, 2026, the entire day is closed with a wind advisory card"
    - "On Thu Jul 9, 2026, the entire day is closed with a wind advisory card"
  artifacts:
    - path: "src/app/api/availability/route.ts"
      provides: "Per-slot block for Jul 7 + full-day weather closures for Jul 8/9"
      contains: "2026-07-07"
  key_links:
    - from: "DATE_BLOCKS['2026-07-07']"
      to: "slot.blocked flag in response"
      via: "blockPredicate(time) with h >= 16 threshold"
      pattern: "2026-07-07.*h >= 16"
    - from: "WEATHER_BLOCKED_DATES['2026-07-08'/'2026-07-09']"
      to: "dateNotice weather card on /book"
      via: "dayWideBlock + dateNotice"
      pattern: "2026-07-0[89].*weather"
---

<objective>
A multi-day wind system is moving across Flathead Lake. Block booking availability
so customers cannot reserve unsafe flights:

- Tue Jul 7, 2026 — block every slot starting at 4:00 PM or later (morning/early-afternoon
  trips stay bookable), and show a wind advisory banner explaining the afternoon closure.
- Wed Jul 8, 2026 — close the entire day with a wind advisory card.
- Thu Jul 9, 2026 — close the entire day with a wind advisory card.

Purpose: Prevent bookings during unsafe wind conditions and communicate clearly to customers.
Output: Edits to the two blocking maps in src/app/api/availability/route.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/app/api/availability/route.ts
@src/components/booking/TimeSlotPicker.tsx

<interfaces>
<!-- The blocking mechanisms already exist in route.ts. Use them exactly as the prior art did. -->

Two per-date maps drive blocking (route.ts ~lines 77-129):

1. DATE_BLOCKS — Record<string, (time: string) => boolean>
   Predicate returns true for slots to DISABLE. Partial-day blocking.
   `to24Hour(time)` converts a "H:MM AM/PM" display string to a 0-23 hour.
   Precedent (Sat Jul 5, commit ce01204):
     '2026-07-05': (t) => { const h = to24Hour(t); return h !== null && h >= 16 && h <= 18; },

2. WEATHER_BLOCKED_DATES — Record<string, { type: 'weather'; message: string }>
   Blocks the WHOLE day (dayWideBlock) AND surfaces a wind-icon closed-day card on /book.
   Precedent (Sat Jul 4, commit 592c2a1):
     '2026-07-04': { type: 'weather', message: '🚩 Red Flag Warning — High Wind 💨' },

3. EVENT_DATES — Record<string, { type: 'event'; emoji; title; message }>
   For a PARTLY-open day, renders as a banner ABOVE the slot grid (TimeSlotPicker.tsx ~line 129).
   Precedent (Fri Jun 19 Father's Day banner) already in the file.

Rendering (TimeSlotPicker.tsx):
- dateNotice.type === 'weather' → full Wind-icon closed-day card (Jul 8/9 path).
- dateNotice.type === 'event' + partly open → banner above bookable slots (Jul 7 path).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Block Jul 7 afternoon + close Jul 8 and Jul 9 for wind</name>
  <files>src/app/api/availability/route.ts</files>
  <action>
Make three additions to the existing maps in the GET handler. Match the existing
formatting (4-space indent, single-line predicates, trailing comma) exactly.

1. In DATE_BLOCKS (after the '2026-07-05' entry, ~line 95), add Tue Jul 7 — block
   ALL slots at 4:00 PM or later using a >= threshold (NOT a hardcoded list, so it
   catches 4/5/6/7 PM and any sunset slot the solar schedule generates):

     // Tue — high wind moving in; block afternoon (4 PM onward), keep morning open
     '2026-07-07': (t) => { const h = to24Hour(t); return h !== null && h >= 16; },

2. In WEATHER_BLOCKED_DATES (after the '2026-07-04' entry, ~line 103), add Wed Jul 8
   and Thu Jul 9 as full-day wind closures. Use a wind-advisory message consistent
   with the Jul 4 precedent's tone:

     '2026-07-08': { type: 'weather', message: '💨 Wind Advisory — All Flights Cancelled' },
     '2026-07-09': { type: 'weather', message: '💨 Wind Advisory — All Flights Cancelled' },

3. In EVENT_DATES (after the '2026-06-21' entry, ~line 128), add Tue Jul 7 so the
   partly-open day shows a banner explaining WHY the afternoon is closed (a partial
   DATE_BLOCK alone shows disabled tiles with no explanation):

     '2026-07-07': {
         type: 'event',
         emoji: '💨',
         title: 'Wind Advisory — Afternoon Closed',
         message: 'High wind is moving in this afternoon — 4 PM and later flights are cancelled. Morning trips are still on, so grab an earlier slot!',
     },

Do NOT change any other dates, the predicate helpers, or the slot-building logic.
  </action>
  <verify>
    <automated>grep -q "'2026-07-07': (t) => { const h = to24Hour(t); return h !== null && h >= 16; }" src/app/api/availability/route.ts && grep -q "'2026-07-08': { type: 'weather'" src/app/api/availability/route.ts && grep -q "'2026-07-09': { type: 'weather'" src/app/api/availability/route.ts && npx tsc --noEmit -p tsconfig.json && echo VERIFIED</automated>
  </verify>
  <done>
Both blocking maps updated. `npx tsc --noEmit` passes. Jul 7 has a DATE_BLOCKS
predicate (h >= 16) and an EVENT_DATES banner entry; Jul 8 and Jul 9 are in
WEATHER_BLOCKED_DATES with the wind advisory message.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Availability blocking for the three wind-affected dates in src/app/api/availability/route.ts:
- Tue Jul 7: 4 PM+ slots disabled, morning open, wind advisory banner.
- Wed Jul 8 & Thu Jul 9: full-day closure with wind advisory card.
  </what-built>
  <how-to-verify>
Start the dev server if it isn't running: `npm run dev`

Optionally spot-check the API directly (returns blocked/dateNotice JSON):
  curl -s 'http://localhost:3000/api/availability?date=2026-07-07' | grep -o '"blocked":[a-z]*'
  curl -s 'http://localhost:3000/api/availability?date=2026-07-08'

Then in the browser at http://localhost:3000/book:
1. Select Tue Jul 7, 2026 → morning/early-afternoon slots (before 4 PM) are bookable;
   every slot at 4 PM and later is disabled; a wind advisory banner shows above the grid.
2. Select Wed Jul 8, 2026 → the whole day shows the Wind-icon "💨 Wind Advisory —
   All Flights Cancelled" closed-day card (no bookable tiles).
3. Select Thu Jul 9, 2026 → same full-day wind advisory card as Jul 8.
4. Sanity check: an unaffected date (e.g. Fri Jul 10) still shows normal open slots.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors introduced).
- /api/availability?date=2026-07-07 returns blocked:true for all 4 PM+ slots and
  blocked:false for earlier slots, plus an event dateNotice.
- /api/availability?date=2026-07-08 and 2026-07-09 return a weather dateNotice and
  all slots blocked.
</verification>

<success_criteria>
- Tue Jul 7: no slot at/after 4 PM is bookable; earlier slots remain bookable; wind
  advisory banner visible.
- Wed Jul 8 & Thu Jul 9: entire day closed with wind advisory card.
- No other dates changed.
</success_criteria>

<output>
After completion, create `.planning/quick/260707-g2h-block-trips-from-4pm-onward-tue-jul-7-an/260707-g2h-SUMMARY.md`
</output>
