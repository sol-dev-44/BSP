---
phase: quick
plan: 260714-e8b
type: execute
wave: 1
depends_on: []
files_modified: [src/app/api/availability/route.ts]
autonomous: true
requirements: [QUICK-260714-e8b]
must_haves:
  truths:
    - "The /book calendar shows July 14, 2026 as fully closed (no bookable slots, including 7 PM)"
    - "July 15, 2026 still shows only its 7 PM trip open (unchanged)"
  artifacts:
    - path: "src/app/api/availability/route.ts"
      provides: "DATE_BLOCKS entry for 2026-07-14 blocking all slots"
      contains: "'2026-07-14': () => true"
  key_links:
    - from: "DATE_BLOCKS['2026-07-14']"
      to: "availability slot generation"
      via: "predicate returns true for every slot"
      pattern: "'2026-07-14': \\(\\) => true"
---

<objective>
Close July 14, 2026 entirely by blocking its remaining 7 PM slot in the availability API. On Jul 13 the 7 PM trip was reopened for both Jul 14 and Jul 15; the owner now wants Jul 14 closed again while leaving Jul 15 untouched.

Purpose: Reflect the owner's updated availability so no customer can book any July 14 flight.
Output: One-line change to the `DATE_BLOCKS` predicate for `2026-07-14`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/api/availability/route.ts

<interfaces>
<!-- Availability blackouts live in the DATE_BLOCKS map inside GET() in
     src/app/api/availability/route.ts. Each entry is a predicate:
     `(time: string) => boolean` — return true for slots to BLOCK.
     A fully-closed day uses `() => true` (see 2026-07-02, 2026-06-20).
     A single-slot-open day uses `h !== <hour24>` (e.g. keep only 7 PM = `h !== 19`).

Current state (lines ~98-103):
    // Mon — only the 6 PM trip runs; block everything else
    '2026-07-13': (t) => { const h = to24Hour(t); return h !== null && h !== 18; },
    // Tue — only the 7 PM trip runs; block everything else
    '2026-07-14': (t) => { const h = to24Hour(t); return h !== null && h !== 19; },
    // Wed — only the 7 PM trip runs; block everything else
    '2026-07-15': (t) => { const h = to24Hour(t); return h !== null && h !== 19; },
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close July 14 by blocking all slots</name>
  <files>src/app/api/availability/route.ts</files>
  <action>
    In the `DATE_BLOCKS` map inside `GET()`, replace the `2026-07-14` entry so the whole day is closed. Change:

        // Tue — only the 7 PM trip runs; block everything else
        '2026-07-14': (t) => { const h = to24Hour(t); return h !== null && h !== 19; },

    to:

        // Tue — closed all day
        '2026-07-14': () => true,

    This mirrors the existing fully-closed pattern used for `2026-07-02` and `2026-06-20`.

    Do NOT touch the `2026-07-15` entry — July 15 keeps only its 7 PM trip open. Do not modify any other DATE_BLOCKS, SOLD_OUT_BLOCKS, WEATHER_BLOCKED_DATES, or EVENT_DATES entries.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "route.ts" | grep -q "^0$" && grep -q "'2026-07-14': () => true" src/app/api/availability/route.ts && grep -q "'2026-07-15': (t) => { const h = to24Hour(t); return h !== null && h !== 19; }" src/app/api/availability/route.ts && echo PASS</automated>
  </verify>
  <done>DATE_BLOCKS['2026-07-14'] is `() => true` (all slots blocked); the 2026-07-15 entry is unchanged; type check passes.</done>
</task>

</tasks>

<verification>
- `grep "'2026-07-14'" src/app/api/availability/route.ts` shows `() => true`
- `grep "'2026-07-15'" src/app/api/availability/route.ts` still shows the `h !== 19` predicate
- No other date entries changed (`git diff` touches only the two lines for 2026-07-14)
</verification>

<success_criteria>
- July 14, 2026 is fully closed on the /book calendar — the 7 PM slot no longer bookable
- July 15, 2026 unchanged (only 7 PM open)
- Change is limited to a single DATE_BLOCKS entry in src/app/api/availability/route.ts
</success_criteria>

<output>
After completion, create `.planning/quick/260714-e8b-block-off-the-7-pm-trip-on-july-14-close/260714-e8b-SUMMARY.md`
</output>
