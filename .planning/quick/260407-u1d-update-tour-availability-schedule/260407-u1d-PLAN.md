---
phase: quick
plan: 260407-u1d
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/booking.ts
  - src/config/solarSchedule.ts
  - src/config/business.ts
  - src/components/booking/DateSelector.tsx
  - src/app/api/availability/route.ts
  - src/components/booking/BookingForm.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Monday, Tuesday, and Thursday show as disabled/closed in the booking calendar"
    - "Saturday and Sunday offer full hours: 10 AM through sunset, on the hour"
    - "Wednesday and Friday offer only 3 PM through sunset, on the hour"
    - "Early bird pricing ($99) only appears on Sat/Sun 10 AM slots"
    - "Wed/Fri first slot (3 PM) is standard pricing ($119), last slot is sunset ($159)"
  artifacts:
    - path: "src/config/solarSchedule.ts"
      provides: "getTimeSlotsForDate accepts optional startHour override for restricted days"
    - path: "src/config/booking.ts"
      provides: "excludedDaysOfWeek includes Mon/Tue/Thu; restricted-hours logic for Wed/Fri"
    - path: "src/config/business.ts"
      provides: "Updated schema.org openingHours for new schedule"
  key_links:
    - from: "src/app/api/availability/route.ts"
      to: "src/config/solarSchedule.ts"
      via: "getTimeSlotsForDate with startHour for Wed/Fri"
      pattern: "getTimeSlotsForDate"
    - from: "src/components/booking/BookingForm.tsx"
      to: "src/config/solarSchedule.ts"
      via: "getTimeSlotsForDate with startHour for Wed/Fri"
      pattern: "getTimeSlotsForDate"
---

<objective>
Update the tour availability schedule so that Saturday and Sunday offer full hours (10 AM to sunset), Wednesday and Friday offer restricted hours (3 PM to sunset only), and Monday/Tuesday/Thursday are closed.

Purpose: Align the booking system with the actual operating schedule for the 2026 season.
Output: All config, API, and UI files reflect the new day-of-week schedule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/config/solarSchedule.ts
@src/config/booking.ts
@src/config/business.ts
@src/components/booking/DateSelector.tsx
@src/components/booking/BookingForm.tsx
@src/app/api/availability/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config layer — solarSchedule.ts, booking.ts, business.ts</name>
  <files>src/config/solarSchedule.ts, src/config/booking.ts, src/config/business.ts</files>
  <action>
**src/config/solarSchedule.ts:**

1. Add an optional `startHour` parameter to `getTimeSlotsForDate`:
   ```
   export function getTimeSlotsForDate(dateStr: string, startHour?: number): string[]
   ```
   In the loop, use `startHour ?? FIRST_TRIP_HOUR` instead of `FIRST_TRIP_HOUR`. This keeps the existing signature backwards-compatible (all current callers pass only dateStr and get the same 10 AM start).

2. Export a new constant for the restricted start hour:
   ```
   export const RESTRICTED_START_HOUR = 15;
   ```

3. Update the module-level JSDoc comment to reflect the new schedule: Sat/Sun full hours (10 AM - sunset), Wed/Fri restricted (3 PM - sunset), Mon/Tue/Thu closed.

4. Update `getScheduleDescription` to accept an optional `startHour` parameter to display the correct range:
   ```
   export function getScheduleDescription(dateStr: string, startHour?: number): string {
       const lastTrip = getLastTripSlot(dateStr);
       const firstHour = startHour ?? FIRST_TRIP_HOUR;
       return `${formatTime(firstHour, 0)} - ${lastTrip}`;
   }
   ```
   Note: `formatTime` is currently a private function. It must remain private (not exported). The `getScheduleDescription` function already has access to it since it's in the same module.

**src/config/booking.ts:**

1. Change `excludedDaysOfWeek: [1]` to `excludedDaysOfWeek: [1, 2, 4]` (Monday=1, Tuesday=2, Thursday=4). Update the comment to: `// Closed Monday, Tuesday, Thursday — open Wednesday, Friday, Saturday, Sunday`.

2. Add a new config property to BOOKING_CONFIG:
   ```
   // Days with restricted hours (3 PM start instead of 10 AM)
   // 0 = Sunday, 3 = Wednesday, 5 = Friday
   restrictedDaysOfWeek: [3, 5] as number[],
   ```

3. Add a helper function:
   ```
   export function isRestrictedDay(dayOfWeek: number): boolean {
       return BOOKING_CONFIG.restrictedDaysOfWeek.includes(dayOfWeek);
   }
   ```

4. Update `getTimeSlotsForDayOfWeek` to pass `RESTRICTED_START_HOUR` for restricted days:
   ```
   import { getTimeSlotsForDate, RESTRICTED_START_HOUR } from './solarSchedule';
   
   export function getTimeSlotsForDayOfWeek(dayOfWeek: number, dateStr?: string): string[] {
       if (dateStr) {
           const startHour = isRestrictedDay(dayOfWeek) ? RESTRICTED_START_HOUR : undefined;
           return getTimeSlotsForDate(dateStr, startHour);
       }
       return BOOKING_CONFIG.timeSlots.daily;
   }
   ```

5. Update the static `timeSlots.daily` comment to note it's a Sat/Sun reference only.

**src/config/business.ts:**

1. Change `openingHours` from `['Tu-Su 10:00-19:00']` to:
   ```
   openingHours: [
       'We,Fr 15:00-19:00',
       'Sa,Su 10:00-19:00',
   ],
   ```
   This is valid schema.org OpeningHoursSpecification format. The 19:00 end time is approximate (varies by sunset); this is acceptable for schema.org.
  </action>
  <verify>
    <automated>cd /Users/alan_campbell_precedent/BSP && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - BOOKING_CONFIG.excludedDaysOfWeek is [1, 2, 4]
    - BOOKING_CONFIG.restrictedDaysOfWeek is [3, 5]
    - getTimeSlotsForDate accepts optional startHour, defaults to 10
    - isRestrictedDay(3) returns true, isRestrictedDay(6) returns false
    - business.ts openingHours reflects We/Fr and Sa/Su separately
  </done>
</task>

<task type="auto">
  <name>Task 2: Update callers — availability API, BookingForm, DateSelector</name>
  <files>src/app/api/availability/route.ts, src/components/booking/BookingForm.tsx, src/components/booking/DateSelector.tsx</files>
  <action>
**src/app/api/availability/route.ts:**

1. Add import: `import { isRestrictedDay } from '@/config/booking';` (already imports BOOKING_CONFIG).
2. Also import `RESTRICTED_START_HOUR` from `@/config/solarSchedule` (add to existing import).
3. Before line 59 (`const dailySlots = getTimeSlotsForDate(date)`), parse the day of week:
   ```
   const dateObj = new Date(date + 'T12:00:00');
   const dayOfWeek = dateObj.getDay();
   const startHour = isRestrictedDay(dayOfWeek) ? RESTRICTED_START_HOUR : undefined;
   const dailySlots = getTimeSlotsForDate(date, startHour);
   ```
   The `T12:00:00` avoids timezone offset issues that could shift the day.

**src/components/booking/BookingForm.tsx:**

1. Add import: `import { isRestrictedDay } from '@/config/booking';` (already imports BOOKING_CONFIG).
2. Also import `RESTRICTED_START_HOUR` from `@/config/solarSchedule` (add to existing import).
3. Around line 57 where `getTimeSlotsForDate(selectedDate)` is called, compute the day of week and pass startHour:
   ```
   const dateObj = new Date(selectedDate + 'T12:00:00');
   const dayOfWeek = dateObj.getDay();
   const startHour = isRestrictedDay(dayOfWeek) ? RESTRICTED_START_HOUR : undefined;
   const solarSlots = getTimeSlotsForDate(selectedDate, startHour);
   ```

**src/components/booking/DateSelector.tsx:**

1. Change the schedule display text on line 129 from:
   `Open Tuesday–Sunday during season`
   to:
   `Open Wed, Fri (3 PM+), Sat & Sun (10 AM+) during season`
  </action>
  <verify>
    <automated>cd /Users/alan_campbell_precedent/BSP && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Availability API returns 3 PM+ slots for Wednesday/Friday dates and 10 AM+ slots for Saturday/Sunday dates
    - BookingForm client-side slot generation matches the same logic
    - DateSelector displays the updated schedule text
    - TypeScript compiles with no errors
    - Next.js build succeeds
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. Manual spot-check: pick a Wednesday date in June 2026 (e.g., 2026-06-03) — the availability API should return slots starting at 3:00 PM, not 10:00 AM
4. Manual spot-check: pick a Saturday date in June 2026 (e.g., 2026-06-06) — slots should start at 10:00 AM as before
5. Manual spot-check: pick a Tuesday date — should be disabled in calendar (excluded day)
</verification>

<success_criteria>
- Mon/Tue/Thu are closed (excludedDaysOfWeek = [1, 2, 4])
- Wed/Fri generate time slots from 3 PM to sunset only
- Sat/Sun generate time slots from 10 AM to sunset (unchanged behavior)
- All pricing tiers still work correctly (earlybird only possible on Sat/Sun 10 AM, sunset on last slot of any open day)
- Build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/quick/260407-u1d-update-tour-availability-schedule/260407-u1d-SUMMARY.md`
</output>
