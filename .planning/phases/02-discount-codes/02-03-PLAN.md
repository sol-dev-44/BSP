---
phase: 02-discount-codes
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/api/create-payment-intent/route.ts
  - src/app/api/discount-codes/validate/route.ts
autonomous: true
requirements:
  - DISC-04
  - DISC-05

must_haves:
  truths:
    - "POST /api/discount-codes/validate returns { valid: true, amount, code_name } for active codes"
    - "POST /api/discount-codes/validate returns { valid: false, error: '...' } for unknown or inactive codes"
    - "POST /api/create-payment-intent accepts optional discount_code field and deducts the discount amount from the Stripe PaymentIntent amount"
    - "Stripe PaymentIntent metadata includes discount_code and discount_amount fields"
  artifacts:
    - path: "src/app/api/discount-codes/validate/route.ts"
      provides: "POST endpoint that checks bsp_discount_codes for active matching code"
      exports: ["POST"]
    - path: "src/app/api/create-payment-intent/route.ts"
      provides: "Updated payment intent creation that applies discount to Stripe amount"
      contains: "discount_code"
  key_links:
    - from: "src/app/api/discount-codes/validate/route.ts"
      to: "bsp_discount_codes"
      via: "supabaseAdmin.from('bsp_discount_codes').select"
      pattern: "bsp_discount_codes"
    - from: "src/app/api/create-payment-intent/route.ts"
      to: "stripe.paymentIntents.create"
      via: "amount after discount deduction"
      pattern: "discount_amount"
---

<objective>
Add server-side discount code validation endpoint and update the payment-intent API to deduct discounts from the Stripe charge.

Purpose: Discount validation must happen server-side so codes cannot be bypassed client-side. The payment intent must reflect the actual (discounted) amount that Stripe charges.
Output: New /api/discount-codes/validate endpoint and updated /api/create-payment-intent that accepts and applies discount codes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/api/create-payment-intent/route.ts
@src/app/api/discount-codes/route.ts
</context>

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From src/app/api/create-payment-intent/route.ts (current POST body):
```typescript
const { party_size, trip_date, trip_time, add_ons } = await request.json();
// Returns: { clientSecret: string }
```

New POST body shape (add optional discount_code):
```typescript
const { party_size, trip_date, trip_time, add_ons, discount_code } = await request.json();
```

bsp_discount_codes shape (from schema):
```typescript
{ id: string, code_name: string, amount: number, is_active: boolean }
```

Validate endpoint contract:
```typescript
// POST /api/discount-codes/validate
// Request:  { code: string }
// Response success: { valid: true, code_name: string, amount: number }
// Response failure: { valid: false, error: string }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create discount code validation endpoint</name>
  <files>src/app/api/discount-codes/validate/route.ts</files>
  <read_first>
    - src/app/api/discount-codes/route.ts — read to see the supabaseAdmin import pattern and bsp_discount_codes table name
    - src/app/api/create-payment-intent/route.ts — read to understand the logging prefix convention ([BSP PAYMENT] style)
  </read_first>
  <action>
Create src/app/api/discount-codes/validate/route.ts as a POST handler:

1. Parse request body: `const { code } = await request.json()`
2. Validate input: if `!code` return `{ valid: false, error: 'No code provided' }` with status 400
3. Normalize code: `const normalized = code.trim().toUpperCase()`
4. Query Supabase: `supabaseAdmin.from('bsp_discount_codes').select('id, code_name, amount, is_active').eq('code_name', normalized).single()`
5. If no data or Supabase error (code not found): return `NextResponse.json({ valid: false, error: 'Invalid discount code' })` with status 200 (not 404 — avoid leaking info)
6. If `!data.is_active`: return `NextResponse.json({ valid: false, error: 'This discount code is no longer active' })` with status 200
7. Return `NextResponse.json({ valid: true, code_name: data.code_name, amount: data.amount })` with status 200

Use `supabaseAdmin` (import from `'@/lib/supabaseAdmin'`). Wrap entire handler in try/catch. Log with `[DISCOUNT]` prefix.
  </action>
  <verify>
    <automated>cd /Users/alan_campbell_precedent/BSP && npx tsc --noEmit 2>&1 | grep "validate" | head -10 || echo "no TypeScript errors for validate route"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/api/discount-codes/validate/route.ts
    - `grep "bsp_discount_codes" src/app/api/discount-codes/validate/route.ts` returns a match
    - `grep "is_active" src/app/api/discount-codes/validate/route.ts` returns a match
    - `grep "valid: true" src/app/api/discount-codes/validate/route.ts` returns a match
    - `grep "valid: false" src/app/api/discount-codes/validate/route.ts` returns at least 2 matches (invalid + inactive cases)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update create-payment-intent to accept and apply discount</name>
  <files>src/app/api/create-payment-intent/route.ts</files>
  <read_first>
    - src/app/api/create-payment-intent/route.ts — read the full current file; understand the existing amount calculation and metadata fields before making changes
  </read_first>
  <action>
Update src/app/api/create-payment-intent/route.ts:

**1. Extend the request body destructuring** to include the optional `discount_code` field:
```typescript
const { party_size, trip_date, trip_time, add_ons, discount_code } = await request.json();
```

**2. Add discount validation block** after the add-ons amount calculation but before the Stripe call.

If `discount_code` is present:
- Normalize: `const normalizedCode = discount_code.trim().toUpperCase()`
- Query Supabase: `supabaseAdmin.from('bsp_discount_codes').select('amount, is_active, code_name').eq('code_name', normalizedCode).single()`
- Import `supabaseAdmin` from `'@/lib/supabaseAdmin'` (add this import)
- If code is found and `is_active`, deduct: `discountAmount = discountData.amount`. Apply: `amount = Math.max(0, amount - discountAmount)` — never go below 0.
- Log: `console.log('[DISCOUNT] Applied discount code', normalizedCode, 'amount', discountAmount)`
- Store `appliedDiscountCode` and `discountAmount` variables for metadata.
- If code not found or inactive: treat as no discount (do not reject — the validate endpoint is separate; payment intent creation is lenient and simply won't discount). Log a warning.

**3. Add discount fields to Stripe PaymentIntent metadata**:
```typescript
metadata: {
  // existing fields...
  discount_code: appliedDiscountCode || '',
  discount_amount: discountAmount || 0,
}
```

**4. Return discount info in response** alongside `clientSecret`:
```typescript
return NextResponse.json({
  clientSecret: paymentIntent.client_secret,
  discountApplied: discountAmount > 0,
  discountAmount: discountAmount || 0,
});
```

Declare `discountAmount = 0` and `appliedDiscountCode = ''` before the conditional block so TypeScript can see they are always initialized.
  </action>
  <verify>
    <automated>cd /Users/alan_campbell_precedent/BSP && npx tsc --noEmit 2>&1 | grep "create-payment-intent" | head -10 || echo "no TypeScript errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "discount_code" src/app/api/create-payment-intent/route.ts` returns a match
    - `grep "discount_amount" src/app/api/create-payment-intent/route.ts` returns a match (in metadata)
    - `grep "supabaseAdmin" src/app/api/create-payment-intent/route.ts` returns a match
    - `grep "Math.max(0" src/app/api/create-payment-intent/route.ts` returns a match (floor at 0)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `npm run build` exits 0
- Manual: POST /api/discount-codes/validate with `{ "code": "TESTCODE" }` returns `{ valid: false, error: "Invalid discount code" }` (no such code yet)
- Manual: POST /api/create-payment-intent with a known valid `discount_code` reduces the amount field in Stripe metadata
</verification>

<success_criteria>
Discount code validation endpoint is live and returns correct valid/invalid responses. Payment intent creation applies the discount to the Stripe charge amount and stores discount metadata. Build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/02-discount-codes/02-03-SUMMARY.md`
</output>
