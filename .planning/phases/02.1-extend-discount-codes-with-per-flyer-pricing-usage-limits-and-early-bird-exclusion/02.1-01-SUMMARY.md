---
phase: 02.1-extend-discount-codes-with-per-flyer-pricing-usage-limits-and-early-bird-exclusion
plan: 01
subsystem: database
tags: [supabase, postgres, plpgsql, rpc, rtk-query, typescript]

requires:
    - phase: 02-discount-codes
      provides: bsp_discount_codes table, DiscountCode RTK interface, code_name uppercase normalization
provides:
    - "Three new bsp_discount_codes columns: max_redemptions, times_redeemed, excludes_early_bird"
    - "bsp_increment_discount_redemption() RPC with atomic conditional UPDATE cap guard (Pattern 2A)"
    - "30OFF inaugural seed code with ON CONFLICT preserving dev test counters"
    - "Extended DiscountCode TypeScript interface for downstream Wave 2 consumers"
    - "Widened addDiscountCode mutation generic accepting optional max_redemptions and excludes_early_bird"
affects: [02.1-02-validate-endpoint, 02.1-03-payment-intent, 02.1-04-admin-ui, 02.1-04-booking-client, 02.1-04-operator-step]

tech-stack:
    added: []
    patterns:
        - "Atomic conditional UPDATE for race-safe counter increment (Pattern 2A)"
        - "ON CONFLICT DO UPDATE preserving server-mutated counter columns (times_redeemed)"
        - "Qualified column names inside RPC body to avoid OUT parameter ambiguity"
        - "Semantic-change documentation via COMMENT ON COLUMN instead of destructive rename (D-01)"

key-files:
    created: []
    modified:
        - "supabase/bsp-schema.sql — appended §10b block (52 new lines) between §10 and §11"
        - "src/lib/api/discountCodesApi.ts — extended DiscountCode interface (3 fields) + widened addDiscountCode generic"

key-decisions:
    - "Preserved bsp_discount_codes.amount column name with COMMENT documenting semantic shift from flat-total to per-guest (D-01) — avoids destructive rename"
    - "max_redemptions defaults to 0 = unlimited so existing test rows remain backward compatible (D-02)"
    - "Counter increment RPC qualifies column names (bsp_discount_codes.times_redeemed) inside WHERE/SET to disambiguate from RETURNS TABLE OUT param of same name"
    - "Seed ON CONFLICT intentionally omits times_redeemed update so dev test counters survive schema re-runs (D-15)"
    - "addDiscountCode mutation generic widened with optional fields — updateDiscountCode left as-is because Partial<DiscountCode> already picks up new fields"

patterns-established:
    - "Pattern 2A — race-safe counter: UPDATE with WHERE cap guard returning new value via RETURNING, GET DIAGNOSTICS row_count to detect at-cap, RETURN QUERY with at_cap flag"
    - "Per-guest discount column: amount semantic preserved by name, changed by COMMENT — downstream consumers must multiply by party_size"

requirements-completed: [DISC-08, DISC-11]

duration: 2min
completed: 2026-05-30
---

# Phase 02.1 Plan 01: Schema Foundation Summary

**bsp_discount_codes extended with per-guest cap + early-bird flag, atomic increment RPC, 30OFF seed, and TypeScript interface mirroring the new schema for Wave 2 consumers.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-05-30T02:06:42Z
- **Completed:** 2026-05-30T02:08:24Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Schema §10b block appended to `supabase/bsp-schema.sql` (52 lines: 3 columns + 3 COMMENTs + RPC function + 30OFF seed)
- `bsp_increment_discount_redemption(p_code_name TEXT)` RPC defined with race-safe conditional UPDATE — returns `(times_redeemed INTEGER, at_cap BOOLEAN)` so Plan 03's bookings route can log cap-exceeded warnings without failing the booking
- 30OFF seed: amount $30/guest, max_redemptions 6, excludes_early_bird true, is_active true — ready for operator to run in Supabase SQL Editor (Plan 04 step)
- `DiscountCode` TypeScript interface extended with 3 new fields so Wave 2 plans (validate route, payment intent, admin UI, booking client) all type-check against a single source of truth
- `addDiscountCode` mutation generic widened to accept optional `max_redemptions` and `excludes_early_bird` for admin POST form (Plan 04)
- `npm run build` exits 0 with the new interface in place

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend supabase/bsp-schema.sql with §10b** — `5229a2d` (feat)
2. **Task 2: Extend DiscountCode interface in discountCodesApi.ts** — `5c1ac2c` (feat)

**Plan metadata:** _pending_ (docs: complete plan)

## Files Created/Modified

- `supabase/bsp-schema.sql` — Inserted §10b block at line 225 (after §10 line 222, before §11 originally at line 224). Contains ALTER TABLE for 3 new columns, 3 COMMENT ON COLUMN statements, the `bsp_increment_discount_redemption` plpgsql function, and the 30OFF INSERT with ON CONFLICT.
- `src/lib/api/discountCodesApi.ts` — Added `max_redemptions: number`, `times_redeemed: number`, `excludes_early_bird: boolean` to `DiscountCode` interface (with inline D-02 comments). Widened `addDiscountCode` mutation generic at line 24 with optional `max_redemptions?: number; excludes_early_bird?: boolean`.

## Decisions Made

None new — all implementation decisions were pre-locked in `02.1-CONTEXT.md` (D-01, D-02, D-10, D-15) and the schema block was pasted verbatim from RESEARCH.md Example 10.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

**Manual operator step deferred to Plan 04 (per plan output spec):**
Before any Plan 02/03 server code runs against the real Supabase database, the operator must execute the §10b block in the Supabase SQL Editor for project `qcohcaavhwujvagmpbdp`. The block is idempotent (ADD COLUMN IF NOT EXISTS + CREATE OR REPLACE FUNCTION + INSERT ... ON CONFLICT) so re-runs are safe.

## Next Phase Readiness

- Wave 2 plans (02.1-02, 02.1-03) can now begin in parallel — the schema shape and TypeScript interface are pinned.
- Plan 02 (validate route) imports the extended `DiscountCode` interface and queries the new columns; it will type-check immediately.
- Plan 03 (payment intent + bookings route counter) calls `supabase.rpc('bsp_increment_discount_redemption', { p_code_name })` — the RPC is defined in the schema file and will be live once Plan 04's operator step runs.
- Plan 04 (admin UI + operator step) reads `max_redemptions` and `times_redeemed` for the "Redemptions" column; the interface now exposes them.

## Self-Check: PASSED

Verified before continuing:

- `supabase/bsp-schema.sql` contains §10b block at line 225 ✓
- All 8 grep acceptance checks for schema pass (columns, RPC, seed, ON CONFLICT) ✓
- `src/lib/api/discountCodesApi.ts` exposes the 3 new interface fields ✓
- `addDiscountCode` mutation generic accepts optional new fields ✓
- `npm run build` exit code 0 ✓
- Commits present: `5229a2d` (Task 1), `5c1ac2c` (Task 2) ✓

---

*Phase: 02.1-extend-discount-codes-with-per-flyer-pricing-usage-limits-and-early-bird-exclusion*
*Completed: 2026-05-30*
