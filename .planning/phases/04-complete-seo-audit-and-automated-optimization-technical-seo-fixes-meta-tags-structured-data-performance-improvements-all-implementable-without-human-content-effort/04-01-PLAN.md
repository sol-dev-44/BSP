---
phase: 04-complete-seo-audit-and-automated-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/sitemap.xml
  - public/robots.txt
  - public/seo-report.json
  - src/config/business.ts
  - src/config/seo.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-03
  - SEO-06
  - SEO-08

must_haves:
  truths:
    - "Crawlers receive the dynamic sitemap from src/app/sitemap.ts, not the stale public/sitemap.xml"
    - "Crawlers receive the dynamic robots.txt from src/app/robots.ts, not the static public/robots.txt"
    - "Google schema.org opening hours say Tu-Su, matching the Monday exclusion in BOOKING_CONFIG"
    - "No invalid google-site-verification meta tag is emitted in the HTML head"
    - "OG image meta tag references a CDN-hosted URL under 300 KB instead of the 2.7 MB local file"
  artifacts:
    - path: "src/config/business.ts"
      provides: "Corrected openingHours"
      contains: "Tu-Su 10:00-19:00"
    - path: "src/config/seo.ts"
      provides: "Cleaned metadata — no verification placeholder, CDN OG image"
  key_links:
    - from: "src/config/seo.ts"
      to: "src/app/layout.tsx"
      via: "BASE_METADATA spread into layout metadata export"
      pattern: "BASE_METADATA"
---

<objective>
Delete stale static SEO files that shadow dynamic App Router routes, fix config-level SEO inaccuracies (opening hours, verification placeholder), and switch OG image to CDN-hosted URL.

Purpose: Crawlers currently receive a stale sitemap with non-existent routes and robots.txt with wrong rules. Schema data tells Google the business is open Monday when it is not. The OG image is 2.7 MB locally served.
Output: Clean SEO foundation — dynamic routes active, accurate schema, fast social previews.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/config/seo.ts
@src/config/business.ts
@src/app/sitemap.ts
@src/app/robots.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stale static SEO files and fix config-level metadata</name>
  <files>public/sitemap.xml, public/robots.txt, public/seo-report.json, src/config/business.ts, src/config/seo.ts</files>
  <read_first>
    - public/sitemap.xml (confirm stale routes like /about, /careers, /charters)
    - public/robots.txt (confirm it exists and conflicts with src/app/robots.ts)
    - public/seo-report.json (confirm it is legacy/stale)
    - src/config/business.ts (find openingHours array at line ~53)
    - src/config/seo.ts (find verification block at line ~55, OG image at line ~29)
    - src/app/sitemap.ts (confirm dynamic sitemap exists and is correct)
    - src/app/robots.ts (confirm dynamic robots exists and is correct)
  </read_first>
  <action>
1. DELETE these three files:
   - `public/sitemap.xml` (stale, references /about, /careers, /charters, /reservations, /theboat which 404)
   - `public/robots.txt` (conflicts with src/app/robots.ts which blocks /api/ and /admin/ correctly)
   - `public/seo-report.json` (legacy report from previous codebase, inaccurate)

2. In `src/config/business.ts`, find the `openingHours` array (line ~53-55). Change:
   ```
   openingHours: [
       'Mo-Su 10:00-19:00',
   ],
   ```
   To:
   ```
   openingHours: [
       'Tu-Su 10:00-19:00',
   ],
   ```
   This aligns schema.org hours with `BOOKING_CONFIG.excludedDaysOfWeek: [1]` (Monday exclusion added in Phase 3).

3. In `src/config/seo.ts`, REMOVE the entire `verification` block (lines ~55-59):
   ```
   verification: {
       google: 'your-google-verification-code', // Add when you get it
       // yandex: 'your-yandex-verification-code',
       // bing: 'your-bing-verification-code',
   },
   ```
   Delete these lines entirely. The placeholder emits an invalid meta tag. Owner can verify via DNS TXT record instead.

4. In `src/config/seo.ts`, update the OG image in the `openGraph.images` array (line ~29) and the `twitter.images` array (line ~43). Change both references from the local 2.7 MB file to the Supabase CDN URL:
   - openGraph.images[0].url: change from `'/colorfulChute.jpg'` to `'https://qcohcaavhwujvagmpbdp.supabase.co/storage/v1/object/public/bsp-images/FlatheadWithShadow.jpg'`
   - twitter.images[0]: change from `'/colorfulChute.jpg'` to `'https://qcohcaavhwujvagmpbdp.supabase.co/storage/v1/object/public/bsp-images/FlatheadWithShadow.jpg'`
   - Update the alt text to: `'Parasailing above Flathead Lake, Montana - Big Sky Parasail'`

   Note: The `next.config.ts` already allows remote images from `qcohcaavhwujvagmpbdp.supabase.co`, so this will work.
  </action>
  <verify>
    <automated>cd /Users/alan_campbell_precedent/BSP && test ! -f public/sitemap.xml && test ! -f public/robots.txt && test ! -f public/seo-report.json && grep -q 'Tu-Su 10:00-19:00' src/config/business.ts && ! grep -q 'your-google-verification-code' src/config/seo.ts && grep -q 'supabase.co' src/config/seo.ts && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - public/sitemap.xml does not exist
    - public/robots.txt does not exist
    - public/seo-report.json does not exist
    - src/config/business.ts contains 'Tu-Su 10:00-19:00' (not 'Mo-Su')
    - src/config/seo.ts does NOT contain 'your-google-verification-code'
    - src/config/seo.ts does NOT contain 'verification' key in BASE_METADATA
    - src/config/seo.ts openGraph.images[0].url contains 'qcohcaavhwujvagmpbdp.supabase.co'
    - src/config/seo.ts twitter.images contains 'qcohcaavhwujvagmpbdp.supabase.co'
  </acceptance_criteria>
  <done>Three stale public files deleted, openingHours corrected to Tu-Su, verification placeholder removed, OG image switched to CDN URL</done>
</task>

<task type="auto">
  <name>Task 2: Verify build succeeds with changes</name>
  <files></files>
  <read_first>
    - src/config/seo.ts (verify the edits from Task 1 are correct TypeScript)
    - src/config/business.ts (verify the edits from Task 1 are correct TypeScript)
  </read_first>
  <action>
Run `npm run build` from the project root to confirm:
- No TypeScript errors from the seo.ts or business.ts edits
- Dynamic sitemap.ts and robots.ts are now the sole sources (no file conflict)
- The build completes without errors

If the build fails, fix any TypeScript issues in seo.ts or business.ts before proceeding.
  </action>
  <verify>
    <automated>cd /Users/alan_campbell_precedent/BSP && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - Build output does not contain "Error" or "error TS"
  </acceptance_criteria>
  <done>Production build succeeds with all config changes applied</done>
</task>

</tasks>

<verification>
- `public/sitemap.xml` no longer exists — `src/app/sitemap.ts` serves `/sitemap.xml`
- `public/robots.txt` no longer exists — `src/app/robots.ts` serves `/robots.txt`
- `openingHours` in business.ts says `Tu-Su` not `Mo-Su`
- No `verification` block in `seo.ts` BASE_METADATA
- OG image URL points to Supabase CDN
- Build succeeds
</verification>

<success_criteria>
- Stale static files removed, dynamic App Router routes unblocked
- Schema.org opening hours match actual Monday exclusion
- No invalid verification meta tag in HTML
- OG image loads from CDN (fast social previews)
- Clean production build
</success_criteria>

<output>
After completion, create `.planning/phases/04-complete-seo-audit-and-automated-optimization-technical-seo-fixes-meta-tags-structured-data-performance-improvements-all-implementable-without-human-content-effort/04-01-SUMMARY.md`
</output>
